apiVersion: v1
kind: Service
metadata:
  name: {{ include "mysite.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "mysite.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
      nodePort: {{ .Values.service.nodePort | default 30085 }}
